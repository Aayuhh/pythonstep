numbers = [10,20,40,30,90,80]
max_val = numbers[0]
min_val = numbers[0]
for num in numbers:
    print("checking",num)
    if num > max_val:
        max_val = num
    if num < min_val:
        min_val = num
        print("maximum:" ,max_val)
        print("minimum:", min_val)